{
  "name": "astro-app",
  "type": "module",
  "version": "0.0.1",
  "main": "electron.cjs",
  "scripts": {
    "dev": "astro dev",
    "dev:4321": "node start-dev-4321.js",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "playwright test",
    "test:headed": "HEADED=true playwright test",
    "test:debug": "HEADED=true playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report",
    "test:electron": "playwright test --config=playwright-electron.config.ts",
    "test:electron:headed": "HEADED=true playwright test --config=playwright-electron.config.ts --headed",
    "electron": "electron .",
    "electron-dev": "NODE_ENV=development electron .",
    "build-electron": "astro build && electron-builder",
    "dist": "astro build && electron-builder --publish=never"
  },
  "dependencies": {
    "@astrojs/node": "^9.4.3",
    "@astrojs/react": "^4.3.0",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9",
    "astro": "^5.13.5",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "electron": "^38.0.0",
    "electron-builder": "^26.0.12",
    "playwright-electron": "^0.5.0"
  }
}
